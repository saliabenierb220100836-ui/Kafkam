[phases.setup]
aptPkgs = ["ffmpeg", "libpq-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn run:app --workers 1 --threads 4 --bind 0.0.0.0:$PORT --timeout 120 --keep-alive 5"
